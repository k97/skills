# Claude Code local settings (machine-specific, not for sharing)
.claude/settings.json
# skills CLI symlink to ~/.agents/skills (absolute path, machine-specific)
.claude/skills

# Local planning docs — kept on disk, not shipped in a skills repo
docs/plans/

# OS / editor cruft
.DS_Store
*.swp
.idea/
.vscode/

# Node (in case a test project lands here)
node_modules/
